name: Run Collector Bot Daily

on:
  schedule:
    - cron: '0 7 * * *'  # UTC 07:00 = Türkiye saatiyle 10:00
  workflow_dispatch:      # Elle başlatma butonu

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Kodları klonla
        uses: actions/checkout@v3

      - name: Python kurulumu
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Kütüphaneleri yükle
        run: pip install requests python-dotenv

      - name: Botu çalıştır
        run: python collector.py

      - name: Değişiklikleri push et
        run: |
          git config user.name "yakup-collector-bot"
          git config user.email "yakup@example.com"
          git add .
          if git diff --cached --quiet; then
            echo "✅ Değişiklik yok, push atlanıyor."
          else
            git commit -m "🔄 Update"
            git push
          fi
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
   run: |
      git diff --cached --quiet && echo "No changes" && exit 0